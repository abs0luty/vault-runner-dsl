/* find key, open the door, or leave via exit */
while true do {
    if on_key  do { pick_key; }
    if at_door { open_door; }
    if at_exit do { break; }
    if front_clear do { move; } else { random_turn; }
}
