// wander until key found
while not on_key do {
    if front_clear do { move; } else { random_turn; }
}
pick_key;

// wander until door found
while not at_door do {
    if front_clear do { move; } else { random_turn; }
}
open_door;
